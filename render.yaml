services:
  - type: web
    name: ologywood
    env: node
    plan: standard
    buildCommand: pnpm install && pnpm build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    healthCheckPath: /health
