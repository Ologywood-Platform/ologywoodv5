// DISABLED: availabilityAlertsService is not available
import { router, protectedProcedure } from "../_core/trpc";
import { z } from "zod";

export const availabilityAlertsRouter = router({
  // All procedures disabled - service not available
});
