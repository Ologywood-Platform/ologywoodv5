/**
 * Semantic Search Router
 * 
 * TODO: This router requires faqs and semanticSearchLogs tables to be created in the database schema.
 * Currently disabled until these tables are implemented.
 */

import { router } from '../_core/trpc';

// This router is empty until the faqs and semanticSearchLogs tables are created
export const semanticSearchRouter = router({});
