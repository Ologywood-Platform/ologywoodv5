import { router } from "../_core/trpc";

// TODO: These tables need to be created in schema before this router can be used
// import { supportCategories, faqs, knowledgeBaseArticles } from "../../drizzle/schema";

// This router is empty until the support/FAQ tables are created in the database schema
export const adminSeedRouter = router({});
