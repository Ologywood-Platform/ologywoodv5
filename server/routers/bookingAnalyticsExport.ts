// DISABLED: bookingAnalyticsExportService is disabled
import { router } from "../_core/trpc";

export const bookingAnalyticsExportRouter = router({
  // All procedures disabled - service not available
});
